import React from "react";
import ReactDOM from "react-dom/client";
import PIContentDashboard from "./App.jsx";
import "./styles/global.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <PIContentDashboard />
  </React.StrictMode>
);
